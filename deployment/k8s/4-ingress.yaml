apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: techbuzz-api-ingress
spec:
  rules:

    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: techbuzz-svc
                port:
                  number: 8080
